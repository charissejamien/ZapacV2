name: zapac
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

# `flutter pub upgrade --major-versions` for automatic dependency updates
dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.2.2

  # Authentication
  firebase_core: ^3.15.2
  firebase_auth: ^5.1.2
  google_sign_in: ^6.2.1
  cupertino_icons: ^1.0.8
  mailer: ^6.5.0
  cloud_functions: 5.6.2
  image_picker: ^1.2.0
  shared_preferences: ^2.0.13
  cloud_firestore: ^5.6.12

  # Maps API
  geolocator: ^14.0.1
  google_maps_flutter: ^2.12.2
  permission_handler: ^12.0.0+1
  http: ^1.2.2
  flutter_polyline_points: ^2.0.0
  web_socket_channel: ^2.4.0

  #Google Fonts
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/Logo.png
    - assets/maxim.png
    - assets/moveit.png
    - assets/joyride.png
    - assets/grab.png
    - assets/grab-white.png
    - assets/angkas.png
    - assets/bus_icon.png

